language: php

sudo: false

php:
  - 7.0
  - 7.1
  - hhvm

cache:
  directories:
    - $HOME/.composer

before_script:
  - phpenv config-rm xdebug
  - composer install

script:
  - vendor/bin/phpunit
